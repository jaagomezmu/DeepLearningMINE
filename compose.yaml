services:
  deeplearning:
    build:
      context: .
      dockerfile: ./config/Dockerfile
    ports:
      - "8888:8888"
    volumes:
      - .:/app
    env_file:
      - ./config/.env
    stdin_open: true
